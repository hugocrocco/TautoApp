server:
  address: 0.0.0.0
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/tauto_core?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

oci:
  objectstorage:
    namespace: axdbbhb5ebzz
    bucket: tauto-user-photos
    region: us-sanjose-1

app:
  admin-key: "hbdt"